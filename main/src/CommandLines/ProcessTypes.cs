namespace ProjNew.CommandLines
{
    /// <summary>
    /// The types which indicates this app should do.
    /// </summary>
    public enum ProcessTypes
    {
        DefGeneration = 0,
        InfoShow,
        ListSdksShow,
        Replacement
    }
}
